# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Signing material: the local code-signing identity lives in the macOS
# keychain, never in the repo (scripts/create-signing-identity.sh generates
# a throwaway keypair and deletes it). Guardrail against accidental commits.
*.pem
*.p12
*.key
*.cer
*.crt
*.mobileprovision

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
