# Raycast script commands

Raycast → Settings → Extensions → Script Commands → Add Directory → pick
this folder. Every command drives the app through its `espresso://` URL
scheme (`on[?for=25m|1h|3600]`, `off`, `toggle`) — the same scheme works
from Apple Shortcuts ("Open URL") and any script.
